apply plugin: 'com.android.application'

android {
    compileSdkVersion 22
    buildToolsVersion '19.1.0'

    defaultConfig {
        applicationId "kr.app.binbang"
        minSdkVersion 11
        targetSdkVersion 22
        versionCode 12
        versionName "1.11"
    }

    signingConfigs {
        debug {
            storeFile file("/Users/imjaehyun/Desktop/BINBANG/keystore.jks")
            storePassword "dla3344"
            keyAlias "binbang"
            keyPassword "dla3344"
//        storeFile file("/Users/juyoungkim/androidsignkey/release.jks")
//            storePassword "123456"
//            keyAlias "myreleasekey"
//            keyPassword "123456"
        }
    }

    buildTypes {
        debug {
            signingConfig signingConfigs.debug
        }
        release {
            minifyEnabled false
            proguardFiles getDefaultProguardFile('proguard-android.txt'), 'proguard-rules.pro'
        }
    }
}

dependencies {
    compile fileTree(dir: 'libs', include: ['*.jar'])
    compile 'com.android.support:appcompat-v7:22.2.0'
    compile project(':universal-image-loader-1.9.3')
    //compile 'com.google.android.gms:play-services:7.5.0'
}
